<UserControl x:Class="Boutique.Views.OutfitCreatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Boutique.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="800"
             d:DesignWidth="1400">
    <Grid Margin="4,0,0,6">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Three-panel layout: Plugins | Armors | Outfit Drafts -->
        <Grid Grid.Row="0" Margin="0,6,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" MinWidth="150" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="2*" MinWidth="300" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" MinWidth="280" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Plugin Selection -->
            <GroupBox Grid.Column="0" Header="Source Plugins" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <TextBox x:Name="PluginSearchBox"
                                 Text="{Binding OutfitPluginSearchText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Filter plugins by name" />
                        <TextBlock Text="🔍 Filter..." Foreground="Gray" IsHitTestVisible="False"
                                   Margin="6,0,0,0" VerticalAlignment="Center"
                                   Visibility="{Binding Text, ElementName=PluginSearchBox, Converter={StaticResource StringEmptyToVisConverter}, ConverterParameter=Inverse}" />
                    </Grid>

                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding FilteredOutfitPlugins}"
                             SelectedItem="{Binding SelectedOutfitPlugin}"
                             SelectionMode="Single"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis" ToolTip="{Binding}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="LightGray" />

            <!-- Middle Panel: Armors from Selected Plugin -->
            <GroupBox Grid.Column="2" Header="Available Armors" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Search:" Style="{StaticResource LabelTextStyle}" />
                        <TextBox Grid.Column="1" Text="{Binding OutfitSearchText, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Column="2"
                                   Text="{Binding OutfitArmors.Count, StringFormat='{}{0} armors'}"
                                   Margin="8,0,0,0" VerticalAlignment="Center" />
                    </Grid>

                    <DataGrid x:Name="OutfitArmorsGrid"
                              Grid.Row="1"
                              ItemsSource="{Binding OutfitArmorsView}"
                              SelectionChanged="OutfitArmorsGrid_SelectionChanged"
                              MouseDoubleClick="OutfitArmorsGrid_MouseDoubleClick"
                              PreviewMouseLeftButtonDown="OutfitArmorsGrid_PreviewMouseLeftButtonDown"
                              MouseMove="OutfitArmorsGrid_MouseMove"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Extended"
                              CanUserSortColumns="True"
                              RowHeight="40">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="" Width="Auto" CanUserSort="False">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="👁" Click="PreviewArmorButton_Click"
                                                Style="{StaticResource IconButtonStyle}"
                                                Width="28" Height="28" FontSize="12" ToolTip="Preview armor" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" Width="*" IsReadOnly="True" Binding="{Binding DisplayName, Mode=OneWay}" />
                            <DataGridTextColumn Header="Slots" Width="100" IsReadOnly="True" Binding="{Binding SlotSummary, Mode=OneWay}" />
                            <DataGridTextColumn Header="Form ID" Width="100" IsReadOnly="True" SortMemberPath="FormIdSortable" Binding="{Binding FormIdDisplay, Mode=OneWay}" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="2"
                            Content="Create New Outfit from Selected"
                            Command="{Binding CreateOutfitCommand}"
                            HorizontalAlignment="Right"
                            Margin="0,4,0,0"
                            Padding="12,6" />
                </Grid>
            </GroupBox>

            <GridSplitter Grid.Column="3" Width="4" HorizontalAlignment="Stretch" Background="LightGray" />

            <!-- Right Panel: Outfit Drafts -->
            <GroupBox Grid.Column="4" Header="Outfit Queue" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="1" Style="{StaticResource DropZoneStyle}" Margin="0,0,0,6"
                            AllowDrop="True"
                            DragEnter="NewOutfitDropZone_OnDragEnter"
                            DragLeave="NewOutfitDropZone_OnDragLeave"
                            DragOver="NewOutfitDropZone_OnDragOver"
                            Drop="NewOutfitDropZone_OnDrop">
                        <TextBlock Text="Drop armors here to create a new outfit (double-click an armor to quick add)."
                                   Style="{StaticResource EmptyStateTextStyle}" TextWrapping="Wrap" />
                    </Border>

                    <Border Grid.Row="0" Style="{StaticResource InfoPanelStyle}" Margin="0,0,0,6"
                            Visibility="{Binding HasExistingPluginOutfits, Converter={StaticResource BoolToVisConverter}}">
                        <StackPanel>
                            <TextBlock Text="{Binding ExistingOutfits.Count, StringFormat='This plugin already defines {0} outfit(s).'}"
                                       FontWeight="Bold" Margin="0,0,0,6" />
                            <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding ExistingOutfits}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Summary}" Foreground="Gray" Margin="0,0,0,2" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <Button Content="Copy Existing Outfits"
                                    Command="{Binding CopyExistingOutfitsCommand}"
                                    HorizontalAlignment="Right" Margin="0,6,0,0" Padding="10,4" />
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="2">
                        <TextBlock Text="No outfits queued. Select armors and click Create Outfit."
                                   Style="{StaticResource EmptyStateTextStyle}"
                                   Visibility="{Binding HasOutfitDrafts, Converter={StaticResource InverseBoolToVisConverter}}" />

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding OutfitDrafts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,6" Padding="6" BorderBrush="#FFD9D9D9" BorderThickness="1" CornerRadius="4"
                                                AllowDrop="True"
                                                DragEnter="OutfitDraftBorder_OnDragEnter"
                                                DragLeave="OutfitDraftBorder_OnDragLeave"
                                                DragOver="OutfitDraftBorder_OnDragOver"
                                                Drop="OutfitDraftBorder_OnDrop">
                                            <StackPanel>
                                                <Grid Margin="0,0,0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Vertical">
                                                        <TextBox Width="180"
                                                                 Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 PreviewTextInput="OutfitNameTextBox_OnPreviewTextInput"
                                                                 DataObject.Pasting="OutfitNameTextBox_OnPasting"
                                                                 MaxLength="40" HorizontalAlignment="Left" />
                                                        <TextBlock Text="{Binding EditorId, Mode=OneWay}"
                                                                   Margin="0,2,0,0" FontSize="11" Foreground="Gray" />
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                                                        <Button Content="👁" Command="{Binding PreviewCommand}"
                                                                Style="{StaticResource IconButtonStyle}"
                                                                Margin="0,0,2,0" FontSize="10" ToolTip="Preview outfit" />
                                                        <Button Command="{Binding RemoveSelfCommand}"
                                                                Style="{StaticResource InlineRemoveButtonStyle}"
                                                                Width="22" Height="22" FontSize="12" ToolTip="Remove outfit" />
                                                    </StackPanel>
                                                </Grid>
                                                <ItemsControl ItemsSource="{Binding Pieces}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,2,0,2">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{Binding SummaryLine}"
                                                                           VerticalAlignment="Center" TextWrapping="NoWrap"
                                                                           TextTrimming="CharacterEllipsis" FontSize="12" />
                                                                <Button Grid.Column="1"
                                                                        Command="{Binding DataContext.RemovePieceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                        CommandParameter="{Binding}"
                                                                        Style="{StaticResource InlineRemoveButtonStyle}"
                                                                        Margin="4,0,0,0" FontSize="9" ToolTip="Remove piece" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <TextBlock Text="No pieces selected." Style="{StaticResource EmptyStateTextStyle}"
                                                           HorizontalAlignment="Left" FontSize="12" Margin="0,2,0,0"
                                                           Visibility="{Binding HasPieces, Converter={StaticResource InverseBoolToVisConverter}}" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="{Binding OutfitDrafts.Count, StringFormat='{}{0} outfit(s) queued'}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1" Command="{Binding SaveOutfitsCommand}" Padding="16,8"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsEnabled="{Binding HasOutfitDrafts}">
                <Grid>
                    <TextBlock Text="Save Outfits" Visibility="{Binding IsCreatingOutfits, Converter={StaticResource InverseBoolToVisConverter}}" />
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsCreatingOutfits, Converter={StaticResource BoolToVisConverter}}">
                        <TextBlock Text="Saving..." Margin="0,0,8,0" />
                        <ProgressBar IsIndeterminate="True" Width="60" Height="12" />
                    </StackPanel>
                </Grid>
            </Button>
        </Grid>
    </Grid>
</UserControl>
