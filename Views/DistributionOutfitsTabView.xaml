<UserControl x:Class="Boutique.Views.DistributionOutfitsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Boutique.ViewModels"
             xmlns:themes="clr-namespace:Boutique.Themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DistributionOutfitsTabViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600"
             d:DesignWidth="1100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Action Buttons and Search -->
        <Grid Grid.Row="0" Margin="0,6,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="↻ Load Outfits" Command="{Binding LoadOutfitsCommand}"
                        Padding="12,4" Margin="0,0,6,0"
                        ToolTip="Load all outfits from the load order" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <CheckBox Content="Hide vanilla distributions" IsChecked="{Binding HideVanillaOutfits}"
                          VerticalAlignment="Center" Margin="0,0,16,0"
                          ToolTip="Hides Outfits with their default outfit as the winning assignment" />
                <TextBlock Text="Search:" Style="{DynamicResource {x:Static themes:ResourceKeys.LabelTextStyleKey}}" />
                <TextBox Text="{Binding OutfitSearchText, UpdateSourceTrigger=PropertyChanged}"
                         MinWidth="250" ToolTip="Search outfits by EditorID, FormKey, or mod" />
            </StackPanel>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="200" MinHeight="100" />
            </Grid.RowDefinitions>

            <!-- Outfits DataGrid -->
            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding FilteredOutfits}"
                      SelectedItem="{Binding SelectedOutfit, Mode=TwoWay}"
                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                      IsReadOnly="True" CanUserSortColumns="True" SelectionMode="Single" SelectionUnit="FullRow"
                      GridLinesVisibility="All" HeadersVisibility="Column"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                      RowHeight="40">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="Background" Value="#400078D4" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="EditorID" Binding="{Binding EditorID}" Width="*" CanUserSort="True" />
                    <DataGridTextColumn Header="FormKey" Binding="{Binding FormKeyString}" Width="150" CanUserSort="True"
                                        ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridFormKeyColumnStyleKey}}" />
                    <DataGridTextColumn Header="Mod" Binding="{Binding ModDisplayName}" Width="150" CanUserSort="True"
                                        ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridSecondaryColumnStyleKey}}" />
                    <DataGridTextColumn Header="NPCs" Binding="{Binding NpcCount}" Width="60" CanUserSort="True" SortDirection="Descending">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right" />
                                <Setter Property="Padding" Value="0,0,8,0" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Preview" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Preview"
                                        Command="{Binding DataContext.PreviewOutfitCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Padding="6,2" HorizontalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="LightGray" Margin="0,2" />

            <!-- Detail Panel -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- NPC Assignments -->
                <GroupBox Grid.Column="0" Header="Distributed to NPCs" Padding="6">
                    <DataGrid ItemsSource="{Binding SelectedOutfitNpcAssignments}"
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                              IsReadOnly="True" CanUserSortColumns="True" SelectionMode="Single" SelectionUnit="FullRow"
                              GridLinesVisibility="All" HeadersVisibility="Column"
                              EnableRowVirtualization="True" EnableColumnVirtualization="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*" CanUserSort="True" />
                            <DataGridTextColumn Header="EditorID" Binding="{Binding EditorId}" Width="180" CanUserSort="True" />
                            <DataGridTextColumn Header="FormKey" Binding="{Binding FormKeyString}" Width="130" CanUserSort="True"
                                                ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridFormKeyColumnStyleKey}}" />
                            <DataGridTextColumn Header="Mod" Binding="{Binding ModDisplayName}" Width="150" CanUserSort="True"
                                                ElementStyle="{DynamicResource {x:Static themes:ResourceKeys.DataGridSecondaryColumnStyleKey}}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>

                <!-- Distribution Details -->
                <GroupBox Grid.Column="2" Header="Distribution Files" Padding="6">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedOutfitNpcAssignments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,4" Padding="6" CornerRadius="2"
                                            Background="#10000000" BorderBrush="LightGray" BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0" Text="{Binding DisplayName}" FontWeight="Bold" />
                                            <ItemsControl Grid.Row="1" ItemsSource="{Binding Distributions}" Margin="0,4,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Margin="0,2" Padding="4" BorderBrush="LightGray" BorderThickness="1">
                                                            <StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding FileName}" FontWeight="Bold" />
                                                                    <TextBlock Text="{Binding FileType, StringFormat=' ({0})'}"
                                                                               Style="{DynamicResource {x:Static themes:ResourceKeys.SecondaryTextStyleKey}}" Margin="4,0,0,0" />
                                                                    <TextBlock Text=" [WINNER]" Foreground="Green" FontWeight="Bold"
                                                                               Visibility="{Binding IsWinner, Converter={StaticResource BoolToVisConverter}}" />
                                                                </StackPanel>
                                                                <TextBlock Text="{Binding TargetingDescription}"
                                                                           Foreground="#8EC07C" FontSize="10" TextWrapping="Wrap" Margin="0,2,0,0"
                                                                           Visibility="{Binding TargetingDescription, Converter={StaticResource StringEmptyToVisConverter}}" />
                                                            </StackPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </GroupBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
